# TODO change once base path changes (https://linear.app/omnidev/issue/OMNI-254/move-apiauth-paths-to-base-path-or-subpath-eg-auth)
AUTH_BASE_URL="https://localhost:8000/api/auth"

# TODO: adjust accordingly for whatever is decided on the confirmation route
CHECKOUT_SUCCESS_URL="https://localhost:3000/confirmation"

# ⚠️ never use in production; disable certificate validation (useful for development environments to avoid having to manually trust a self-signed certificate)
# reference: https://nodejs.org/api/cli.html#node_tls_reject_unauthorizedvalue
NODE_TLS_REJECT_UNAUTHORIZED="0"
